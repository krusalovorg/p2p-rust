# Light Protocol for Layered Peer Network (LP2LN)

## Обзор
------------

**LP2LN** - это децентрализованная P2P-сеть для безопасного обмена файлами и ресурсами между пользователями. Проект реализует современную архитектуру peer-to-peer с поддержкой сквозного шифрования, обходом NAT, распределенным хранением данных и веб-интерфейсом.

### Ключевые особенности

- 🔐 **Сквозное шифрование** - AES, ECDSA, TLS
- 🌐 **Обход NAT и файрволов** - STUN/TURN протоколы
- 📁 **Распределенное хранение** - фрагментация и репликация файлов
- 🚀 **Высокая производительность** - асинхронная архитектура на Rust
- 🎯 **Веб-интерфейс** - современный UI для управления файлами
- 🔌 **Chrome Extension** - кошелек для работы с сетью
- 🐳 **Docker поддержка** - легкое развертывание

## Архитектура проекта
-------------------

```
P2P-Server/
├── src/                    # Основной код на Rust
│   ├── main.rs            # Точка входа приложения
│   ├── peer/              # P2P клиент и API
│   ├── signal/            # Сигнальный сервер
│   ├── manager/           # Управление соединениями
│   ├── crypto/            # Криптографические функции
│   ├── http/              # HTTP API и прокси
│   ├── contract/          # Смарт-контракты (WASM)
│   ├── db/                # База данных (Redb)
│   ├── tunnel/            # P2P туннели
│   └── ui/                # Консольный интерфейс
├── site/                  # Веб-интерфейс
├── wallet/                # Chrome Extension кошелек
├── contracts/             # Смарт-контракты
├── modules/               # Python модули
└── tools/                 # Вспомогательные инструменты
```

## Компоненты системы
-------------------

### 1. P2P Сервер (Rust)
- **Peer** - основной P2P клиент
- **Signal Server** - сервер для обнаружения пиров
- **Connection Manager** - управление соединениями
- **Tunnel** - создание P2P туннелей
- **HTTP Proxy** - веб-прокси для доступа к файлам

### 2. Веб-интерфейс
- Современный UI на HTML/CSS/JavaScript
- Загрузка и управление файлами
- Мониторинг сети
- Статистика соединений

### 3. Chrome Extension
- Кошелек для работы с сетью
- Управление ключами
- Просмотр файлов
- Интеграция с Ethereum

### 4. Смарт-контракты
- WASM runtime для выполнения контрактов
- Распределенная логика
- Токены и метаданные

## Начало работы
---------------

### Предварительные требования

- **Rust** 1.70+ и Cargo
- **Node.js** 18+ (для веб-компонентов)
- **Docker** (опционально)
- **Python** 3.10+ (для модулей)

### Быстрый старт

1. **Клонирование репозитория:**
```bash
git clone <repository-url>
cd P2P-Server
```

2. **Сборка проекта:**
```bash
cargo build --release
```

3. **Настройка конфигурации:**
```bash
# Редактируйте config.toml
signal_server_ip = "127.0.0.1"
signal_server_port = 3031
storage_size = 2147483648  # 2 ГБ
proxy_port = 80
```

4. **Запуск:**
```bash
# Запуск как P2P клиент
cargo run

# Запуск как сигнальный сервер
cargo run -- --signal

# С указанием пути к БД
cargo run -- --db-path ./data
```

### Docker развертывание

1. **Сборка образа:**
```bash
docker build -t lp2ln-server .
```

2. **Запуск контейнера:**
```bash
docker run -d \
  --name lp2ln-server \
  -p 3031:3031 \
  -p 80:80 \
  -p 8080:8080 \
  -p 8081:8081 \
  -e CONTAINER_TYPE=signal \
  lp2ln-server
```

3. **Автоматический деплой:**
```bash
chmod +x docker-deploy.sh
./docker-deploy.sh
```

## Использование
-------------

### P2P Клиент

После запуска клиент автоматически:
- Подключается к сигнальному серверу
- Обнаруживает других пиров
- Создает P2P туннели
- Обрабатывает входящие соединения

### Веб-интерфейс

Откройте браузер и перейдите на `http://localhost:80`:
- Загрузка файлов в сеть
- Просмотр доступных файлов
- Мониторинг соединений
- Управление настройками

### Chrome Extension

1. Установите расширение из `wallet/dist.crx`
2. Создайте кошелек
3. Импортируйте ключи
4. Начните работу с сетью

## Конфигурация
-------------

### Основные параметры (config.toml)

```toml
# Сигнальный сервер
signal_server_ip = "127.0.0.1"
signal_server_port = 3031

# Хранилище
storage_size = 2147483648  # 2 ГБ

# HTTP прокси
proxy_ip = "127.0.0.1"
proxy_port = 80
```

### Командная строка

```bash
# Основные опции
P2P-Server.exe [OPTIONS]

Options:
  --signal          Запуск как сигнальный сервер
  --db-path <FILE>  Путь к директории базы данных
  -h, --help        Показать справку
```

## Разработка
-----------

### Структура кода

- **src/peer/** - P2P клиент и API
- **src/signal/** - Сигнальный сервер
- **src/manager/** - Управление соединениями
- **src/crypto/** - Криптографические функции
- **src/http/** - HTTP API и прокси
- **src/contract/** - WASM runtime для контрактов

### Зависимости

Основные зависимости Rust:
- `tokio` - асинхронный runtime
- `rustls` - TLS шифрование
- `redb` - встроенная база данных
- `k256` - криптографические алгоритмы
- `wasmtime` - WASM runtime
- `hyper` - HTTP сервер

### Тестирование

```bash
# Запуск всех тестов
cargo test

# Запуск конкретных тестов
cargo test db_tests
cargo test peer_api_tests
```

## Безопасность
-------------

- **Сквозное шифрование** всех данных
- **ECDSA** подписи для аутентификации
- **AES** шифрование для файлов
- **TLS** для защищенных соединений
- **STUN/TURN** для обхода NAT
- **Анонимность** - нет сбора метаданных

## Производительность
-------------------

- **Асинхронная архитектура** на Tokio
- **Многопоточность** для параллельной обработки
- **Оптимизированная сеть** с минимальными задержками
- **Распределенное хранение** для высокой доступности
- **Кэширование** для быстрого доступа

## Мониторинг
------------

### Логи

Логи сохраняются в `logs/` директории:
```
logs/log_2025-07-25_15-41-37.log
```

### Метрики

- Количество активных соединений
- Статистика передачи данных
- Состояние туннелей
- Производительность сети

## Планы развития
--------------

### Краткосрочные цели
- [ ] Улучшение UI/UX веб-интерфейса
- [ ] Оптимизация производительности
- [ ] Расширение API
- [ ] Мобильное приложение

### Долгосрочные цели
- [ ] Интеграция с блокчейн сетями
- [ ] Поддержка IoT устройств
- [ ] Mesh-сети для мобильных устройств
- [ ] Глобальная сеть узлов

## Вклад в проект
--------------

1. Форкните репозиторий
2. Создайте ветку для новой функции
3. Внесите изменения
4. Добавьте тесты
5. Создайте Pull Request

## Лицензия
---------

Проект распространяется под лицензией, указанной в файле `LICENSE`.

## Поддержка
----------

- **Issues**: GitHub Issues
- **Discussions**: GitHub Discussions
- **Email**: [contact@lp2ln.com]

---

**LP2LN** - будущее децентрализованных сетей уже здесь! 🚀
